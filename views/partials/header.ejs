<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/main.css" />
    <style>
      html,
      body {
        height: 100%;
        margin: 0;
        display: flex;
        flex-direction: column;
      }

      main {
        flex-grow: 1;
      }

      .header {
        background-color: #007bff;
        padding: 10px 0;
      }

      .header-title {
        color: #fff;
      }

      .header h1 {
        font-size: 1.5em;
      }
      footer {
        flex-shrink: 0;
        background-color: #007bff;
        color: #fff;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px 20px;
      }

      footer p {
        margin: 0;
        margin-left: 10px;
        font-size: 16px;
      }

      footer img {
        max-width: 149px;
        height: auto;
        margin-left: 20px;
      }

      .nav-link {
        margin-right: 15px;
      }

      .nav-link:hover {
        color: #007bff;
      }

      .text-left {
        text-align: left;
        margin-left: 10px;
      }

      .channel-name a {
        text-decoration: none;
        color: inherit; /* Може да зададеш желан цвят */
      }

      .channel-name a:hover {
        text-decoration: underline;
      }

      .channel-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 20px;
        margin-top: 20px;
      }

      .channel-card {
        background-color: #f9f9f9;
        border: 2px solid #ddd;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .channel-card:hover {
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        border-color: #007bff;
      }

      .button {
        position: relative;
        display: inline-block;
        margin: 15px;
        padding: 10px 20px;
        text-align: center;
        font-size: 16px;
        letter-spacing: 1px;
        text-decoration: none;
        color: #007bff;
        background: transparent;
        cursor: pointer;
        transition: ease-out 0.5s;
        border: 2px solid #007bff;
        border-radius: 10px;
        box-shadow: inset 0 0 0 0 #007bff;
      }

      .button:hover {
        color: white;
        box-shadow: inset 0 -100px 0 0 #007bff;
      }

      .button:active {
        transform: scale(0.9);
      }

      .hidden {
        display: none;
      }

      .delete-button {
        color: #cc0000;
        border: 2px solid #cc0000;
        box-shadow: inset 0 0 0 0 #cc0000;
        transition: background-color 0.3s, box-shadow 0.5s;
      }

      .delete-button:hover {
        color: #fff;
        background-color: #cc0000;
        box-shadow: inset 0 -100px 0 0 #cc0000; /* Червено подчертаване */
      }

      .delete-button:active {
        transform: scale(0.9);
      }

      .btn-grop {
        display: flex;
        flex-direction: column;
      }

      .header-button {
        position: relative;
        display: inline-block;
        margin: 10px 5px;
        padding: 10px 20px;
        text-align: center;
        font-size: 16px;
        letter-spacing: 1px;
        text-decoration: none;
        color: white;
        background: transparent;
        cursor: pointer;
        transition: ease-out 0.3s;
        border: 2px solid white;
        border-radius: 5px;
        box-shadow: inset 0 0 0 0 transparent;
      }

      .header-button:hover {
        color: #444cf7;
        background: white;
        box-shadow: inset 0 -100px 0 0 transparent;
      }

      .header-button:active {
        transform: scale(0.9);
      }

      /* Стил за съобщението за грешка */
      .error-message {
        background-color: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
        padding: 15px;
        border-radius: 5px;
        margin-bottom: 20px;
      }
      .search-button {
        position: relative;
        display: inline-block;
        margin: 15px;
        padding: 15px 30px;
        text-align: center;
        font-size: 16px;
        letter-spacing: 1px;
        color: #fff;
        background-color: #444cf7;
        cursor: pointer;
        transition: background-color 0.3s, ease-out 0.5s, box-shadow 0.3s;
        border: none;
        border-radius: 10px;
        box-shadow: inset 0 0 0 0 #444cf7;
      }

      .search-button:hover {
        color: #fff;
        box-shadow: inset 0 -100px 0 0 #444cf7;
      }

      .search-button:active {
        transform: scale(0.9);
      }
     
      .login-button {
    position: absolute;
    right: 10px;

}

.create-channel-button {
    position: absolute;
    right: 140px;
}

      .error-message strong {
        font-weight: bold;
      }

      @keyframes banter {
        0% {
          transform: scale(1);
        }
        100% {
          transform: scale(0.5);
        }
      }
      .search-container {
        display: none;
        background-color: #f8f9fa;
        padding: 5px;
        border-radius: 5px;
      }

      .search-container.active {
        display: block;
      }

      .search-input input {
        width: 200px;
        border: 1px solid #ddd;
        padding: 5px;
      }

     .searchBox {
    display: flex;
    max-width: 345px; /* Увеличаваме ширината с 50% (230px * 1.5) */
    margin: 0 auto; /* Центриране на търсачката */
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    background: #fff;
    border-radius: 50px;
    border: 2px solid #000;
    position: relative;
}

.searchIconButton {
    color: white;
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    width: 50px;
    height: 50px;
    display: flex; /* За центриране на SVG */
    justify-content: center;
    align-items: center;
    border-radius: 50%;
    background: #444cf7;
    border: 0;
    transition: all 300ms cubic-bezier(0.23, 1, 0.32, 1);
}

.searchButton:hover {
    color: #fff;
    background-color: #1a1a1a;
    box-shadow: rgba(0, 0, 0, 0.5) 0 10px 20px;
    transform: translateY(-50%) translateX(-3px);
}

.searchButton:active {
    box-shadow: none;
    transform: translateY(-50%);
}

.searchInput {
    flex-grow: 1; /* Позволява на полето за търсене да заема повече място */
    border: none;
    background: none;
    outline: none;
    color: black;
    font-size: 15px;
    padding: 12px 16px; /* Регулиране на вътрешния отстъп */
}
      .header-image {
        width: 149px;
        height: auto;
        margin: 0 10px;
      }
    </style>
    <title>KnowSpace</title>
  </head>
  <body>
    <!-- Хедър с класове от Bootstrap -->
    <header class="header p-3 mb-3 border-bottom">
      <div class="container">
        <div
          class="d-flex flex-wrap justify-content-center justify-content-lg-start"
        >
          <a
            href="/"
            class="d-flex align-items-center mb-2 mb-lg-0 text-dark text-decoration-none"
          >
            <img
              src="/images/KnowSpaceHeader.png"
              alt="Image description"
              class="header-image"
            />
          </a>

          <ul
            class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0"
          >
            <li>
              <button onclick="window.location.href='/'" class="header-button">
                Начално
              </button>
            </li>
            <li>
              <button
                onclick="window.location.href='/new-channel'"
                class="header-button create-channel-button"
              >
                Създай нов канал
              </button>

              <button onclick="" class="header-button login-button">
                Влизане
              </button>
            </li>
          </ul>
        </div>
      </div>
    </header>

    <main>
